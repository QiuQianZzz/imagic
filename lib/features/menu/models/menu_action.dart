enum MenuAction {
  openFile,
  save,
  saveAs,
  closeFile,
  exit,
  undo,
  redo,
  copy,
  actualSize,
  fitToWindow,
  zoomIn,
  zoomOut,
  fullscreen,
  togglePanel,
  editMode,
  openSettings,
  about,
}
